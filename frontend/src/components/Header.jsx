import { Card, CardHeader, CardTitle } from "./ui/card";

const Header = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle className='text-center'>Business Card App</CardTitle>
        </CardHeader>
      </Card>
    </div>
  );
};

export default Header;
